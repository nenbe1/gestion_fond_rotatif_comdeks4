/**
 * Palette reprise de frontend/src/App.css (:root) pour garder la même
 * identité visuelle entre le Web et le Mobile.
 */
export const couleurs = {
  vertFonce: '#2c4a3a',
  vertMoyen: '#4a7a5e',
  orMil: '#c9a24b',
  creme: '#faf8f3',
  blanc: '#ffffff',
  grisTexte: '#2a2a28',
  grisClair: '#e5e1d8',
  brique: '#a8442e',
  bleuAttente: '#6b7f9e',

  // Teintes claires (fonds de badges), reprises des classes .badge-* du Web (App.css)
  vertMoyenClair: '#e6f2ea',   // fond badge Confirmé/Soldé (associé à vertMoyen)
  bleuAttenteClair: '#eef1f6', // fond badge En attente (associé à bleuAttente)
  orMilClair: '#fdf3e0',       // fond badge Remboursement en cours (associé à orMil)
  briqueClair: '#fbe9e6',      // fond badge Rejeté/Erreur (associé à brique)
};
